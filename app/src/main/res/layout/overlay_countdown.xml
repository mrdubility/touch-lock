<?xml version="1.0" encoding="utf-8"?>
<!--
  倒计时阶段的顶部小卡片浮层：
  - 窗口高度只有卡片这么高，卡片以外的触摸自然落到底层应用，用户可正常切换到要保护的应用；
  - 服务侧不加 FLAG_NOT_TOUCHABLE，因此卡片上的"取消 / 设置"按钮可以点；
  - 不设 screenBrightness，倒计时期间保持正常亮度便于操作。
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/overlay_scrim"
    android:padding="12dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_countdown_card"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="12dp">

        <TextView
            android:id="@+id/countdownSecondsText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:minWidth="44dp"
            android:textColor="@color/countdown_text_primary"
            android:textSize="28sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/countdownHintText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:layout_weight="1"
            android:text="@string/countdown_card_hint"
            android:textColor="@color/countdown_text_secondary"
            android:textSize="13sp" />

        <Button
            android:id="@+id/cancelButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:minWidth="0dp"
            android:paddingStart="14dp"
            android:paddingEnd="14dp"
            android:text="@string/countdown_cancel"
            android:textSize="13sp" />

        <ImageButton
            android:id="@+id/settingsButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginStart="4dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/countdown_settings"
            android:padding="8dp"
            android:scaleType="centerInside"
            android:src="@drawable/ic_settings"
            android:tint="@color/countdown_text_primary" />

    </LinearLayout>

</FrameLayout>
